name: Notify Slack

on:
  push:
    branches:
      - main  # hoặc branch bạn muốn

jobs:
  slack_notification:
    runs-on: ubuntu-latest

    steps:
      - name: Send Slack Notification
        env:
          SLACK_WEBHOOK_URL: ${https://hooks.slack.com/services/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}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "✅ Có một push mới lên nhánh *main*! 🚀"
            }' $SLACK_WEBHOOK_URL
